<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd">

    <context:property-placeholder file-encoding="UTF-8"
                                  location="classpath*:example/local-mq-amq-example.properties"/>

    <bean id="tcListener" class="com.ysu.zyw.tc.components.mq.amq.TcQueueListener1">
        <property name="rethrowException" value="false"/>
    </bean>

    <!-- topic destination -->
    <bean id="topicDestination" class="org.apache.activemq.command.ActiveMQTopic">
        <constructor-arg value="topic"/>
    </bean>

    <bean class="org.springframework.jms.listener.DefaultMessageListenerContainer">
        <property name="connectionFactory" ref="connectionFactory"/>
        <property name="taskExecutor" ref="mqThreadPoolTaskScheduler"/>
        <property name="destination" ref="topicDestination"/>
        <property name="messageListener" ref="tcListener"/>
        <property name="concurrentConsumers" value="1"/>
        <property name="messageConverter" ref="mqMessageConverter"/>
    </bean>

    <bean class="org.springframework.jms.listener.DefaultMessageListenerContainer">
        <property name="connectionFactory" ref="connectionFactory"/>
        <property name="taskExecutor" ref="mqThreadPoolTaskScheduler"/>
        <property name="destination" ref="topicDestination"/>
        <property name="messageListener" ref="tcListener"/>
        <property name="concurrentConsumers" value="1"/>
        <property name="messageConverter" ref="mqMessageConverter"/>
    </bean>

    <!-- queue destination -->
    <bean id="queueDestination" class="org.apache.activemq.command.ActiveMQQueue">
        <constructor-arg value="queue"/>
    </bean>

    <bean class="org.springframework.jms.listener.DefaultMessageListenerContainer">
        <property name="connectionFactory" ref="connectionFactory"/>
        <property name="taskExecutor" ref="mqThreadPoolTaskScheduler"/>
        <property name="destination" ref="queueDestination"/>
        <property name="messageListener" ref="tcListener"/>
        <property name="concurrentConsumers" value="1"/>
        <property name="messageConverter" ref="mqMessageConverter"/>

    </bean>

</beans>